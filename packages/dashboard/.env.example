# public URL the browser uses to start github oauth
NEXT_PUBLIC_API_URL=http://localhost:3001

# server-side URL (falls back to NEXT_PUBLIC_API_URL)
# API_URL=http://localhost:3001

# marketing site link shown on the sign-in page
NEXT_PUBLIC_SITE_URL=https://devdrip.xyz
